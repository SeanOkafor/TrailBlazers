# Implementation Documentation

## Project: Trail Blazers
**Repository:** https://github.com/SeanOkafor/GameDevAssignment1.git

---

## Running the Application

### Prerequisites
- Java JDK 21 (or compatible version)
- Git (for version control)

### Compilation
To compile the project from the root directory:

```powershell
cd src
javac util/*.java
javac *.java
cd ..
```

### Running the Game
From the project root directory:

```powershell
java -classpath src MainWindow
```

### Game Controls
- **W** - Move up
- **A** - Move left
- **S** - Move down
- **D** - Move right
- **Space** - Shoot

---

## Project Structure
```
BasicGameTemplate/
├── src/
│   ├── MainWindow.java    - Main entry point, screen navigation, and game loop
│   ├── Level1Panel.java   - Level 1 parallax scrolling background (mountain theme)
│   ├── Level2Panel.java   - Level 2 parallax scrolling background (industrial theme)
│   ├── Player1.java       - Player 1 animated sprite, movement, combat & shooting
│   ├── Player2.java       - Player 2 animated sprite, movement, combat & shooting
│   ├── Projectile.java    - Animated projectile (regular & special attacks)
│   ├── TutorialEnemy.java - Tutorial boss enemy (Level 1 only)
│   ├── FirstBoss.java     - First boss with 2 phases (Level 1, after tutorial)
│   ├── FinalBoss.java     - Final boss with 3 phases (Level 2 only)
│   ├── ScoreScreen.java   - Post-boss score overlay
│   ├── Controller.java    - Handles keyboard input (singleton pattern)
│   ├── Model.java         - Game logic and state
│   ├── Viewer.java        - Rendering and display
│   └── util/
│       ├── GameObject.java
│       ├── Point3f.java
│       ├── Vector3f.java
│       └── UnitTests.java
├── res/
│   ├── New Graphics/
│   │   ├── Main Screen.png
│   │   ├── ControlsScreen.png
│   │   ├── Level Selection.png
│   │   ├── ScoreScreen.png
│   │   ├── Player1Sprites/
│   │   ├── Player2Sprites/
│   │   ├── Tutorial Enemy/
│   │   ├── Final Boss/
│   │   ├── parallax_mountain_pack/
│   │   └── parallax-industrial-pack/
│   └── Music/
│       ├── awesomeness.wav
│       ├── level1music.wav
│       └── Orbital Colossus.wav
├── IMPLEMENTATION.md  - This file
├── REQUIREMENTS.md    - Game design document
└── bin/               - Compiled classes (gitignored)
```

---

## Game: Trail Blazers
A horizontal side-scrolling game inspired by Jetpack Joyride and Geometry Dash. The game features a multi-screen navigation system with pre-designed screen images and transparent button overlays.

---

## Implementation Changes

### Initial Setup (February 19, 2026)
- Initialized Git repository
- Created .gitignore for Java projects
- Connected to GitHub repository
- Initial commit with basic MVC game template

### Screen Navigation System (February 26, 2026)

#### Overview
Implemented a full screen navigation system with three screens (Main Screen, Controls Screen, Level Selection Screen) using transparent JButton overlays positioned on top of pre-designed PNG images. Each screen image is scaled from its original resolution to fit the 1000x1000 game window.

#### Window & Title
- Changed JFrame title from "Basic Game Engine" to **"Trail Blazers"**
- Window size: 1000x1000 pixels
- Layout: null (absolute positioning for precise button placement)

#### Image Scaling Approach
All screen images are loaded via `ImageIO.read()`, scaled to 1000x1000 using `Image.getScaledInstance()`, and displayed as `JLabel` with `ImageIcon`. Button coordinates from the original image are mathematically scaled to match the displayed size.

- **Main Screen & Controls Screen** (1152x928 originals): `x_scale = 1000/1152`, `y_scale = 1000/928`
- **Level Selection Screen** (1344x800 original): `x_scale = 1000/1344`, `y_scale = 1000/800`

#### Transparent Button Pattern
All buttons use the same styling to be invisible overlays on the screen artwork:
```java
setOpaque(false);
setContentAreaFilled(false);
setBorderPainted(false);
setFocusPainted(false);
setCursor(HAND_CURSOR);
```

#### Screens & Navigation Flow

```
Main Screen
├── [Controls Button]      → Controls Screen
├── [Single Player Button] → Level Selection Screen (multiplayerEnabled = false)
└── [Multiplayer Button]   → Level Selection Screen (multiplayerEnabled = true)

Controls Screen
└── [Back Button]          → Main Screen

Level Selection Screen
├── [Level 1 Button]       → Level 1 (mountain parallax)
├── [Level 2 Button]       → Level 2 (industrial parallax)
└── [Back Button]          → Main Screen
```

#### Button Coordinates (scaled bounds)

| Button | Screen | Original Coords | Image Size | Scaled Bounds (x, y, w, h) |
|--------|--------|-----------------|------------|---------------------------|
| Controls | Main | (912,832)→(1124,897) | 1152x928 | (792, 897, 184, 70) |
| Single Player | Main | (263,833)→(560,900) | 1152x928 | (228, 897, 258, 72) |
| Multiplayer | Main | (587,833)→(852,900) | 1152x928 | (509, 897, 230, 72) |
| Back | Controls | (529,770)→(622,798) | 1152x928 | (459, 830, 81, 30) |
| Level 1 | Level Selection | (63,232)→(653,587) | 1344x800 | (47, 290, 439, 444) |
| Level 2 | Level Selection | (690,233)→(1282,587) | 1344x800 | (513, 291, 441, 443) |
| Back | Level Selection | (626,730)→(718,759) | 1344x800 | (466, 913, 68, 36) |

#### Multiplayer Toggle
- `private static boolean multiplayerEnabled = false;` — toggled by Single Player / Multiplayer buttons
- `public static boolean isMultiplayerEnabled()` — getter for use by other classes (e.g., Model)
- Single Player sets it to `false`, Multiplayer sets it to `true`
- Both buttons navigate to the same Level Selection screen

#### Key Methods Added to MainWindow.java
- `showMainScreen()` — Shows main screen + its 3 buttons, hides everything else
- `showControlsScreen()` — Shows controls screen + back button, hides everything else
- `showLevelSelectionScreen()` — Shows level selection + back button, hides everything else

#### Files Modified
- **MainWindow.java** — Removed old "Start Game" button, added screen navigation system with 3 screens and 5 transparent buttons

#### Files Created
- **REQUIREMENTS.md** — Full game design document with sprite checklist, mechanics, and milestones

### February 26, 2026 — Parallax Backgrounds (Level 1 & Level 2)
- Created `Level1Panel.java` — 5-layer mountain parallax scrolling background
- Created `Level2Panel.java` — 4-layer industrial parallax scrolling background
- Added Level 1 and Level 2 buttons on Level Selection screen
- Both panels animate at ~100 FPS via the main game loop

### February 26, 2026 — Menu Music
- Added looping background music (`awesomeness.wav`) on menu screens
- Stops on level entry, resumes on return to main screen

### February 26, 2026 — Level 1 Music
- Added looping music (`level1music.wav`) for Level 1

### February 28, 2026 — Level 2 Music
- Added looping music (`Orbital Colossus.wav`) for Level 2

### February 27, 2026 — Player Sprites
- Created `Player1.java` and `Player2.java` with 4-frame flight animations
- W/S controls for P1, Up/Down for P2
- Multiplayer toggle creates/removes Player 2

### February 28, 2026 — Game Stats, Timer & Scoring
- HP system (5 HP per player per level, hearts HUD)
- Damage tracking per player
- Level timer (MM:SS, top-centre)
- Score formula: `(damage × 100) − (seconds × 5)`, min 0
- All stats reset on level entry

### March 1, 2026 — Attack System (Regular & Special Projectiles)
- Created `Projectile.java` — 3-frame ping-pong animated projectile
- Regular attack: 100 damage, 60×30 display, G (P1) / L (P2)
- Special attack: 1000 damage, 150×75 display, auto-fires after 1500 charge
- Press-only input (no hold-to-fire)
- Projectile list managed per level panel

### March 1, 2026 — Tutorial Enemy (Level 1 Boss)
- Created `TutorialEnemy.java` — bobs up/down, 3 damage-state sprites
- 5000 HP single / 10000 HP multiplayer
- Death slide off right edge, health bar, red damage flash
- AABB collision with collision insets (100px per side)

---

## Bug Fix: Tutorial Enemy Hitbox Too Large (March 1, 2026)

### Problem
The Tutorial Enemy's collision hitbox used the full 395×395 display area, but the actual
sprite content (the block) only fills roughly the centre ~195×195 pixels. This caused
projectiles to register hits in the transparent padding surrounding the sprite.

### Fix (TutorialEnemy.java)
- Added `COLLISION_INSET_X` and `COLLISION_INSET_Y` constants (100 px each) that define
  the transparent padding on each side of the sprite image.
- Updated `collidesWith()` to compute a tighter hitbox:
  ```
  hitX = x + COLLISION_INSET_X
  hitY = y + COLLISION_INSET_Y
  hitW = DISPLAY_WIDTH  - 2 * COLLISION_INSET_X   (= 195)
  hitH = DISPLAY_HEIGHT - 2 * COLLISION_INSET_Y   (= 195)
  ```
- AABB check now uses the inset rectangle instead of the full display area.

---

## Final Boss (Level 2) — 3-Phase Boss (March 2, 2026)

### Overview
The Final Boss appears only in Level 2. It has **3 phases**, each with **25,000 HP**
(total health bar: **75,000 HP**). Each phase is essentially a separate character with
its own animated sprites, sharing a single persistent health bar.

### Sprites
Each phase has 3 looping animation frames (stored in separate subfolders):
- `res/New Graphics/Final Boss/phase1/FBphase1-{1,2,3}.png`
- `res/New Graphics/Final Boss/phase2/FBphase2-{1,2,3}.png`
- `res/New Graphics/Final Boss/phase3/FBphase3-{1,2,3}.png`

### State Machine
The boss cycles through 5 states:
| State | Description |
|-------|-------------|
| **ENTERING** | Descends from above the screen to the centre (3 px/frame) |
| **ACTIVE** | On screen, animating, can be damaged by projectiles |
| **SLIDING** | Phase HP depleted → slides off right edge (5 px/frame) |
| **WAITING** | Off screen, 60-frame pause before next phase enters |
| **DEFEATED** | All 3 phases beaten, boss removed |

### Phase Transitions
1. Player deals 25,000 damage → current phase HP hits 0
2. Health bar **locks** (no more damage during transition)
3. Current phase slides off the right edge of the screen
4. Brief pause (~0.6 s)
5. Next phase descends from the top into position
6. Boss becomes ACTIVE again — health bar unlocks

### Health Bar
- 350×24 bar above the boss sprite, showing total HP / 75,000
- Colour: green (>50%) → yellow (20-50%) → red (<20%)
- "Phase X / 3" label drawn above the bar

### Damage Flash
Same SRC_IN red-tint overlay technique as the Tutorial Enemy (45% alpha, 10 frames).

### Collision
- Same inset AABB approach as Tutorial Enemy (100 px inset on each side)
- Only registers hits when boss is in ACTIVE state
- Projectile collision updates firing player's damage dealt + special charge

### Files Created
- **FinalBoss.java** — Full boss class with 3-phase state machine, animated sprites,
  health bar, damage flash, collision detection

### Files Modified
- **Level2Panel.java** — Added `FinalBoss` field, `spawnFinalBoss()` method,
  boss update/draw calls, projectile-boss collision in the update loop
- **MainWindow.java** — `showLevel2()` now calls `level2Panel.spawnFinalBoss(multiplayerEnabled)`

---

## Score Screen — Post-Boss Results Overlay (March 3, 2026)

### Overview
After a level's boss is defeated, a score screen image (`ScoreScreen.png`, ~470×339)
appears centred on the 1000×1000 panel. It displays each player's damage done, time
taken, and total score. Stats are revealed one row at a time (1 second apart) for
dramatic effect. Press **SPACE** to return to the main menu.

### Score Formula
```
totalScore = (damageDealt × 10) − (timeInSeconds × 2000)
minimum score = 0
```

### Staggered Reveal (at 100 FPS)
| Delay | Row |
|-------|-----|
| 1 second (100 frames) | Damage Done (P1 + P2) |
| 2 seconds (200 frames) | Time Taken (same for both) |
| 3 seconds (300 frames) | Total Score (P1 + P2) |

### Text Positions (relative to image top-left)
| Stat | P1 Box | P2 Box |
|------|--------|--------|
| Damage | (124,119)–(215,140) | (366,119)–(457,140) |
| Time | (124,153)–(215,174) | (366,153)–(457,174) |
| Score | (130,276)–(217,295) | (371,276)–(458,295) |

### Trigger
- **Level 1**: FirstBoss's `isDefeated()` returns true (both phases beaten)
- **Level 2**: Final Boss's `isDefeated()` returns true (all 3 phases beaten)

### Controls
- **SPACE**: Returns to main menu (only after all 3 rows are revealed)

### Stat Resets
All stats (damage dealt, special charge, HP, timer, score screen state) reset when
entering a level via `resetPosition()` + `resetTimer()`.

### Files Created
- **ScoreScreen.java** — Loads `ScoreScreen.png`, manages reveal timer, draws stats
  in the correct text box positions

### Files Modified
- **Level1Panel.java** — Added `ScoreScreen` field + `scoreScreenTriggered` flag,
  triggers on first boss defeat, draws overlay, getter for MainWindow
- **Level2Panel.java** — Same as Level1Panel but triggers on Final Boss defeat
- **MainWindow.java** — Added `VK_SPACE` handler in KeyEventDispatcher that calls
  `showMainScreen()` when the score screen is fully revealed

---

## First Boss (Level 1) — 2-Phase Boss (March 3, 2026)

### Overview
After the Tutorial Enemy (the block) is defeated in Level 1, the First Boss descends
from the top of the screen. It has **2 phases**, each with **30,000 HP** (total: **60,000 HP**).
In multiplayer, HP is doubled to **60,000 per phase** (total: **120,000 HP**).

### Level 1 Flow
```
Level Start → Tutorial Enemy (block) → First Boss (2 phases) → Score Screen
```

### Sprites
Each phase has 3 looping animation frames (189×249 native resolution):
- `res/New Graphics/first boss/phase 1/level1phase1-{1,2,3}.png`
- `res/New Graphics/first boss/phase 2/level1phase2-{1,2,3}.png`

### State Machine (same pattern as Final Boss)
| State | Description |
|-------|-------------|
| **ENTERING** | Descends from above the screen to the centre (3 px/frame) |
| **ACTIVE** | On screen, animating, can be damaged by projectiles |
| **SLIDING** | Phase HP depleted → slides off right edge (5 px/frame) |
| **WAITING** | Off screen, 60-frame pause before next phase enters |
| **DEFEATED** | Both phases beaten, triggers score screen |

### Health Bar
- 300×24 bar above the boss sprite, showing total HP / 60,000
- Colour: green (>50%) → yellow (20-50%) → red (<20%)
- "Phase X / 2" label drawn above the bar

### Collision
- AABB with 30px insets on each side (hitbox: 129×189)
- Only registers hits when boss is in ACTIVE state

### Auto-Spawn
The First Boss spawns automatically inside Level1Panel when `tutorialEnemy.isDefeated()`
becomes true. No MainWindow changes needed.

### Shared Collision Helper
Extracted a `trackProjectileHit(Projectile p)` method in Level1Panel that handles
damage tracking and special charge for both the tutorial enemy and first boss collisions.

### Files Created
- **FirstBoss.java** — 2-phase boss: state machine, animated sprites, health bar,
  damage flash, collision detection (modelled on FinalBoss.java)

### Files Modified
- **Level1Panel.java** — Added `FirstBoss` field + `firstBossSpawned` flag,
  auto-spawns first boss on tutorial enemy defeat, collision detection for
  projectiles vs first boss, score screen now triggers on first boss defeat
  (not tutorial enemy defeat). Extracted `trackProjectileHit()` helper.

---

## Pending Work
- First Boss attacks (AI behaviour per phase)
- Final Boss AI and attacks for each phase
- Additional enemies/obstacles

---
